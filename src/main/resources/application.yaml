spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/binbang
    username: user
    password: password1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

# JWT 설정
jwt:
  secret: but-im-a-creep-im-a-weirdo-what-the-hell-am-i-doin-here-i-dont-belong-here
  access-token-expiration: 1800000      # 30분 (밀리초)
  refresh-token-expiration: 604800000  # 7일 (밀리초)

